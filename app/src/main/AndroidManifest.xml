<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.edu.uniandes.vinilos">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_vinilos_square"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_vinilos_circle"
        android:supportsRtl="true"
        android:theme="@style/Theme.Vinilos">
        <activity
            android:name=".view.collector.CollectorDetailActivity"
            android:exported="false" />
        <activity
            android:name=".view.album.AddTrackAlbumActivity"
            android:exported="false" />
        <activity
            android:name=".view.album.AlbumCreateActivity"
            android:exported="false" />
        <activity
            android:name=".view.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Vinilos.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.album.DetailAlbumActivity" />
        <activity android:name=".view.artist.ArtistDetailActivity" />
    </application>

</manifest>